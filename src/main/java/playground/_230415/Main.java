/*
package playground._230415;
public class Main {
    public static void main(String[] args ){
        String data = "hello world";
        byte[] bytes = data.getBytes("utf-8");

        InputStream is = new ByteArrayInputStream(bytes,0,bytres.length);
        BufferedInputStream bis = new Bis(is, 8192);
        InputStreamReader isr = new ISR(bis, "utf-8");

        char[] cBuffer = new char[1024];
        StringBuild sb = new StirngBuild();
        while(true){
            int len = isr.read(cBuffer);
            if(len == -1 ){
                break;
            }
            sb.append(cBuffer, 0 , len);
        }
        print(sb);

    }

}
*/
